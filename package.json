{
  "name": "noc-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx rimraf ./dist/ && npx tsc",
    "start": "npm run build && tsnd --respawn ./dist/app.js",
    "start:dev": "npx tsnd --respawn ./src/app.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/typescript": "^2.0.0",
    "cron": "^3.1.7",
    "dotenv": "^16.4.5",
    "env-var": "^7.5.0",
    "nodemon": "^3.1.3",
    "ts-node-dev": "^2.0.0"
  }
}
